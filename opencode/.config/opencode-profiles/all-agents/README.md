All-agents profile

This profile contains repo custom agents that are excluded from the baseline
OpenCode path.

Config file:
`opencode/.config/opencode-profiles/all-agents/opencode.json`

Custom agents directory:
`opencode/.config/opencode-profiles/all-agents/agents/`
